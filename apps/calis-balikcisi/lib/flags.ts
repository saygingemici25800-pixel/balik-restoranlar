// Özellik bayrakları (kod sabiti — env değil).
//
// RES_ENABLED: Rezervasyon sistemi (route, nav linki, anasayfa CTA) geçici
// olarak devre dışı. Kod/dosyalar silinmedi; yalnızca bu bayrak gizliyor.
// Geri açmak için tek değişiklik: `true` yap.
export const RES_ENABLED = false;
